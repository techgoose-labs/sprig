# dependencies + build artifacts
node_modules/
_fresh/
test-results/
.DS_Store

# sprig build output — self-contained in one folder (no stray manifest)
/static/

# generated sprig previews (isolate generates these per discovered case)
/app/src/_preview/
/app/src/pages/_preview/
# the project's design tokens, forwarded into the workbench per isolate run
/app/src/css-variables.json

# scaffolding kept locally, not shipped — the old code lives in git history
/reference/
/spec/

# skill eval workspaces (not shipped — regenerated by eval runs)
skills/deno-fresh2-workspace/
skills/design-workspace/

# local scratch (playwright MCP profile, screenshots)
.playwright-mcp/
isolate-gallery.png

# merged dev/isolate config (generated by `sprig dev`/`isolate` withMergedConfig)
/.sprig-app.json

# infra: downloaded env files (secrets — never commit)
/env/

# box shell aliases rm→trash and drops these next to the cwd; one got committed in d1f47e1
.Trash-*/
